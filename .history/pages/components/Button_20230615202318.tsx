'use client'
interface ButtonProps {
    label: string;
    onClick: (e: React.MouseEvent<HTMLButtonElement>) => void;
    disab
}

const Button = () => {
  return (
    <button></button>
  )
}

export default Button